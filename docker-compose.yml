version: '3'
services:
  web:
    build: .
    volumes:
      - ./data:/data:ro
    ports:
      - "5000:5000"
  nginx:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web
